内部 HTTP 通知投递服务 — 技术设计方案

一、背景与目标

企业内部多个业务系统在关键事件发生时，需要调用外部供应商 HTTPS API 进行通知。不同供应商 API 地址、Header、Body 格式各不相同。业务系统不关心返回值，只需确保通知被可靠送达。

核心目标： 设计一个中继服务，接收业务系统的通知请求，尽可能可靠地投递到目标地址（at-least-once）。

 ---
二、整体架构

┌─────────────────────────────────────────────────────────────────────────────────┐
│                        通知中继服务 (Notification Relay)                          │
│                                                                                 │
│                                                                                 │
│  ┌──────────────┐      ┌───────────────────┐      ┌────────────────────────┐   │
│  │  接入层 API  │─────▶│  内存延迟队列      │─────▶│  投递 Worker 线程池    │   │
│  │              │      │  (DelayQueue)      │      │                        │   │
│  └──────┬───────┘      └───────────────────┘      └───────────┬────────────┘   │
│         │                       ▲                              │                │
│         │ 持久化                │ 失败重入队(带延迟)            │ HTTP 请求      │
│         ▼                       │                              ▼                │
│  ┌──────────────┐      ┌───────┴───────────┐      ┌────────────────────────┐   │
│  │   MySQL      │      │  重试调度器        │      │  外部供应商 HTTPS API  │   │
│  │  (持久化层)  │◀─────│  (状态回写)        │      └────────────────────────┘   │
│  └──────────────┘      └───────────────────┘                                   │
│         ▲                                                                       │
│         │ 启动时恢复未完成任务                                                    │
│         │                                                                       │
│  ┌──────┴───────┐                                                               │
│  │  启动恢复器  │                                                               │
│  │  (Recovery)  │                                                               │
│  └──────────────┘                                                               │
└─────────────────────────────────────────────────────────────────────────────────┘

核心设计思路：内存驱动 + DB 持久化

关键原则：不轮询数据库。 正常运行时所有调度都在内存中完成，MySQL 只做持久化保障。

数据流：

1. 请求接入：业务系统 POST 通知 → 接入层校验 + 写入 MySQL（status=PENDING）→ 立即投入内存延迟队列 → 返回 202
2. 首次投递：Worker 从内存队列取出任务 → 发送 HTTP 请求 → 成功则回写 MySQL（status=DELIVERED）
3. 失败重试：投递失败 → 计算退避延迟 → 任务重新放入内存延迟队列（带延迟时间）→ 同时回写 MySQL（status=RETRYABLE, next_attempt_at）
4. 重试耗尽：达到最大重试次数 → 回写 MySQL（status=FAILED）
5. 启动恢复：服务重启时，从 MySQL 加载所有未完成任务（PENDING/RETRYABLE 且 next_attempt_at <= now）到内存队列

 ---
三、为什么不轮询 DB

┌─────────────────────┬────────────────────┬──────────────────┬──────────────────────┐
│        方案         │        延迟        │     DB 压力      │        扩展性        │
├─────────────────────┼────────────────────┼──────────────────┼──────────────────────┤
│ DB 轮询（每 500ms） │ 最大 500ms         │ 高（持续查询）   │ 多实例需 SKIP LOCKED │
├─────────────────────┼────────────────────┼──────────────────┼──────────────────────┤
│ 内存延迟队列        │ 接近 0（直接入队） │ 低（仅读写状态） │ 单实例内完成调度     │
└─────────────────────┴────────────────────┴──────────────────┴──────────────────────┘

内存队列方案下：
- 正常流量：任务从 API 接收直接进入内存队列，Worker 立即消费，全程不查 DB
- 失败重试：任务带延迟时间重入内存队列，到期后 Worker 自动取出，不查 DB
- 唯一需要读 DB 的场景：服务启动时恢复未完成任务

持久化保障： 每个任务在进入内存队列之前，一定先写入 MySQL。即使进程崩溃，重启后可从 DB 恢复所有未完成任务，不丢消息。

 ---
四、核心组件设计

4.1 内存延迟队列

使用 Java DelayQueue<DelayedTask> 或类似结构：

- 首次提交的任务：delay = 0（立即可消费）
- 重试任务：delay = 指数退避计算值
- Worker 线程池（N 个线程）阻塞式从 DelayQueue 取任务，取到即投递

好处：
- 无轮询，事件驱动
- 重试延迟精确（不依赖轮询间隔）
- CPU 开销极低（阻塞等待，不空转）

4.2 投递 Worker

- 从 DelayQueue 中阻塞获取任务
- 发送 HTTP 请求到 target_url
- 根据结果更新状态：
    - 2xx → DELIVERED
    - 可重试失败（5xx/429/超时/连接异常）→ 重入队列（带退避延迟）
    - 不可重试失败（4xx 如 400/404）→ FAILED
    - 达到最大重试次数 → FAILED

4.3 启动恢复器（Recovery）

服务启动时执行一次：

SELECT * FROM notification_task
WHERE status IN ('PENDING', 'RETRYABLE')

将这些任务加载到内存延迟队列：
- 如果 next_attempt_at <= NOW()：delay = 0，立即投递
- 如果 next_attempt_at > NOW()：delay = next_attempt_at - NOW()

这是唯一一次读取 DB 作为调度用途。

4.4 水平扩展考量

单实例模式下，内存队列天然无竞争。如果需要多实例：
- 方案 A：接入层通过负载均衡 + 粘性路由（按 task_uid hash），每个实例处理固定分区
- 方案 B：引入 Redis 作为共享延迟队列（ZSET + score=执行时间），替代 JVM 内存队列
- 方案 C：保持单实例主备模式（对通知场景的吞吐量通常足够）

本期推荐方案 C（单实例），架构预留方案 B 的扩展路径。

 ---
五、数据库设计（单表）

notification_task

┌──────────────────┬─────────────────────────────────────────────────┬────────────────────────────────┐
│       字段       │                      类型                       │              说明              │
├──────────────────┼─────────────────────────────────────────────────┼────────────────────────────────┤
│ id               │ BIGINT AUTO_INCREMENT PK                        │ 自增主键                       │
├──────────────────┼─────────────────────────────────────────────────┼────────────────────────────────┤
│ idempotency_key  │ VARCHAR(256) NOT NULL UNIQUE                    │ 幂等键（全局唯一，调用方必填） │
├──────────────────┼─────────────────────────────────────────────────┼────────────────────────────────┤
│ source_system    │ VARCHAR(64) NOT NULL                            │ 来源系统标识                   │
├──────────────────┼─────────────────────────────────────────────────┼────────────────────────────────┤
│ event_type       │ VARCHAR(128) NOT NULL                           │ 业务事件类型                   │
├──────────────────┼─────────────────────────────────────────────────┼────────────────────────────────┤
│ target_url       │ TEXT NOT NULL                                   │ 目标 HTTPS 地址                │
├──────────────────┼─────────────────────────────────────────────────┼────────────────────────────────┤
│ http_method      │ VARCHAR(10) NOT NULL DEFAULT 'POST'             │ HTTP 方法                      │
├──────────────────┼─────────────────────────────────────────────────┼────────────────────────────────┤
│ headers          │ JSON NOT NULL DEFAULT '{}'                      │ 请求头                         │
├──────────────────┼─────────────────────────────────────────────────┼────────────────────────────────┤
│ body             │ MEDIUMTEXT                                      │ 请求体                         │
├──────────────────┼─────────────────────────────────────────────────┼────────────────────────────────┤
│ content_type     │ VARCHAR(128) DEFAULT 'application/json'         │ Content-Type                   │
├──────────────────┼─────────────────────────────────────────────────┼────────────────────────────────┤
│ timeout_ms       │ INT DEFAULT 10000                               │ 单次请求超时（ms）             │
├──────────────────┼─────────────────────────────────────────────────┼────────────────────────────────┤
│ status           │ VARCHAR(16) NOT NULL DEFAULT 'PENDING'          │ 见下方状态机                   │
├──────────────────┼─────────────────────────────────────────────────┼────────────────────────────────┤
│ attempt_count    │ INT NOT NULL DEFAULT 0                          │ 已尝试次数                     │
├──────────────────┼─────────────────────────────────────────────────┼────────────────────────────────┤
│ max_attempts     │ INT NOT NULL DEFAULT 5                          │ 最大尝试次数                   │
├──────────────────┼─────────────────────────────────────────────────┼────────────────────────────────┤
│ next_attempt_at  │ DATETIME NOT NULL DEFAULT NOW()                 │ 下次尝试时间                   │
├──────────────────┼─────────────────────────────────────────────────┼────────────────────────────────┤
│ last_status_code │ INT                                             │ 最近响应码                     │
├──────────────────┼─────────────────────────────────────────────────┼────────────────────────────────┤
│ last_error       │ VARCHAR(512)                                    │ 最近错误摘要                   │
├──────────────────┼─────────────────────────────────────────────────┼────────────────────────────────┤
│ created_at       │ DATETIME NOT NULL DEFAULT NOW()                 │ 创建时间                       │
├──────────────────┼─────────────────────────────────────────────────┼────────────────────────────────┤
│ updated_at       │ DATETIME NOT NULL DEFAULT NOW() ON UPDATE NOW() │ 更新时间                       │
└──────────────────┴─────────────────────────────────────────────────┴────────────────────────────────┘

状态机：

PENDING ──首次投递──▶ DELIVERED（终态）
│
│ 失败且未达上限
▼
RETRYABLE ──重试成功──▶ DELIVERED（终态）
│
│ 达到 max_attempts
▼
FAILED（终态）

索引：
- UNIQUE(idempotency_key) — 幂等
- idx_recovery (status, next_attempt_at) — 仅用于启动恢复

 ---
六、重试策略

指数退避 + 抖动

delay = min(initialDelay × 2^(attempt-1) + jitter, maxDelay)

默认值：
initialDelay = 1s
multiplier = 2
jitter = random(0, delay × 0.2)
maxDelay = 1 小时
maxAttempts = 5

失败分类

┌────────────────────────────────────┬────────────┬──────────────────┐
│             HTTP 响应              │    分类    │       动作       │
├────────────────────────────────────┼────────────┼──────────────────┤
│ 2xx                                │ 成功       │ status=DELIVERED │
├────────────────────────────────────┼────────────┼──────────────────┤
│ 429, 500, 502, 503, 504            │ 暂时性失败 │ 重入队列 + 退避  │
├────────────────────────────────────┼────────────┼──────────────────┤
│ 连接超时 / 连接拒绝 / DNS 解析失败 │ 网络异常   │ 重入队列 + 退避  │
├────────────────────────────────────┼────────────┼──────────────────┤
│ 400, 404, 405, 422                 │ 永久性失败 │ status=FAILED    │
├────────────────────────────────────┼────────────┼──────────────────┤
│ 达到 max_attempts                  │ 重试耗尽   │ status=FAILED    │
└────────────────────────────────────┴────────────┴──────────────────┘

 ---
七、可靠性保障

7.1 不丢消息

- 先持久化，后入队：任务写入 MySQL 成功后才放入内存队列。即使入队前崩溃，重启恢复可兜底
- 先投递，后更新状态：投递成功后才标记 DELIVERED。即使标记前崩溃，重启后会重新投递（at-least-once）

7.2 进程崩溃恢复

- 启动恢复器加载所有 PENDING/RETRYABLE 任务到内存队列
- 已经 DELIVERED/FAILED 的不会被重新投递

7.3 幂等保证

- idempotency_key 全局唯一约束
- 重复提交直接返回已有任务状态，不会产生重复投递
- 调用方有责任生成全局唯一的幂等键（如 {event_type}:{business_id}:{timestamp}）

7.4 任务超时保护

- 如果某个任务长时间处于 PENDING 状态（内存队列丢失但 DB 已写入），启动恢复机制兜底
- 可选：增加定时检查（如每 10 分钟检查一次是否有"滞留"任务），作为防御性兜底，而非主要调度手段

 ---
八、技术选型

┌─────────────┬───────────────────────────────────────────────────┐
│    组件     │                       选型                        │
├─────────────┼───────────────────────────────────────────────────┤
│ 语言/框架   │ Java 17 + Spring Boot 3.x                         │
├─────────────┼───────────────────────────────────────────────────┤
│ 数据库      │ MySQL 8.x                                         │
├─────────────┼───────────────────────────────────────────────────┤
│ 内存队列    │ Java DelayQueue（或 ScheduledThreadPoolExecutor） │
├─────────────┼───────────────────────────────────────────────────┤
│ HTTP 客户端 │ Spring WebClient（非阻塞、连接池）                │
├─────────────┼───────────────────────────────────────────────────┤
│ 数据库迁移  │ Flyway                                            │
└─────────────┴───────────────────────────────────────────────────┘

 ---
九、实施步骤

1. 项目脚手架 + MySQL 建表
2. 接入层 API（接收、校验、持久化、入队）
3. 内存延迟队列 + Worker 线程池
4. 投递逻辑 + 退避计算 + 状态回写
5. 启动恢复器
6. 集成测试（模拟成功/失败/重试/崩溃恢复场景）